# upgrade-artifacts (sample)

Minimal execution-artifacts repo layout:

deviceName/YYYY/runId/
  - checks.json            (committed twice: PRE then POST)
  - backups/
      - running-config.txt
      - startup-config.txt

This sample includes a few devices and runIds to demonstrate structure.
